@startuml
' Beállítások a BME-s analízis modell stílushoz
skinparam classAttributeIconSize 0
skinparam monochrome true

' --- Interfészek ---

interface ILepheto {
  + idoEltelt(): void
}

interface ICsuszhat {
    + kicsuszik() : void
    + utkozott(masik : Jarmu) : void
}

' --- Közös osztályok és Enumok (A csapattárs alapján) ---

enum UtTipus {
    NORMAL
    ALAGUT
    HID
    KERESZTEZODES
}

enum Epulet {
    MEGALLO
    HAZ
    MUNKAHELY
    BAZIS
}

abstract class Jarmu {
  - pozicio: Utszakasz
  + {abstract} halad(cel: Utszakasz): void
  + getPozicio(): Utszakasz
}

class Utszakasz {
    -ho : int
    -jeg : int
    -havonAthaladt : int
    -kozlekedoJarmu : Jarmu
    -felrehuzodottJarmu : Jarmu
    -szakaszElore : Utszakasz
    -szakaszBalra : Utszakasz
    -szakaszJobbra : Utszakasz
    +idojarasFrissites() : void
    +foglaltE() : boolean
    +foglaltSagValtoztatas(foglaltsag : boolean, jarmu : Jarmu) : void
}

' Enumok bekötése az Utszakaszba (Aggregáció)
Utszakasz o-- "1" UtTipus 
Utszakasz o-- "0..1" Epulet 

' --- Saját domén (Busz és Játékos) ---

abstract class Jatekos {
  - nev: String
  - egyenleg: int
  + penztKap(osszeg: int): void
  + getEgyenleg(): int
}

class Buszvezeto {
  - sajatBusz: Busz
  + utvonalatValtoztat(ujCel: Utszakasz): void
}

class Busz {
  - celAllomas: Utszakasz
  - hatralevoJavitasiIdo: int
  - vezerlo: Buszvezeto
  + halad(cel: Utszakasz): void
  + utkozott(masik: Jarmu): void
  + kicsuszik(): void
  + celbaErt(): void
  + idoEltelt(): void
}

' --- Kapcsolatok ---

Jatekos <|-- Buszvezeto
Jarmu <|-- Busz

' Interfész megvalósítások
ILepheto <|.. Busz
ICsuszhat <|.. Busz

' Asszociáció a Busz és a vezetője között
Busz "1" -right- "1" Buszvezeto 

' A Busz és az Útszakasz közötti kapcsolatok
Busz --> "1" Utszakasz : celAllomas >
Buszvezeto ..> Utszakasz : kijelöli >
@enduml
